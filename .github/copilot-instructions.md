Spawn multiple subagents for parallelizsble subtasks.

Use latest Claude Sonnet model, or, if unavailable, Auto Mode.

plan all tasks from issues in a PR before waiting for a go-live (keyword execute by repo maintainers).

until implementation phase, do continuous PRs with at least one model of: claude, copilot, or @gemini-code-assist; until nneenode.
